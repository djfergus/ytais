# Required: OpenRouter API key for summary generation
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Model to use for generating summaries
SUMMARY_MODEL=x-ai/grok-4.1-fast:free

# Optional: Enable or disable summary generation (true/false)
ENABLE_SUMMARY=true

# Optional: Summary generation timeout in seconds
SUMMARY_TIMEOUT=120

# Optional: Maximum retry attempts for API calls
MAX_RETRIES=3

# Optional: Delay between retries in seconds
RETRY_DELAY=2